---

- name: Update package repositories
  become: true
  apt:
    update_cache: True
  tags:
    - apt
    - update_cache

- name: Install common package dependencies
  become: true
  apt:
    name: "{{ item }}"
    state: present
  register: install_common_package_dependencies
  when: _common_package_dependencies is defined
  with_items: common_package_dependencies
  tags:
    - apt
    - dependencies
- debug: var=install_common_package_dependencies.stdout_lines

- name: Install common dependencies
  become: true
  apt:
    name: "{{ item.name }}"
    state: present
  register: install_common_dependencies
  when: _common_dependencies is defined
  with_items: _common_dependencies
  tags:
    - apt
    - dependencies
- debug: var=install_common_dependencies.stdout_lines
